void main(){
  var age=45;
  print(age--); //unary postfix operator[ storing the updated value in age variable but prints the previous value]
  print(age);// printing the updated value of age variable
  print(age++); //unary postfix operator [storing the value in age variable but prints the previous value]
  print(age); // printing the updated value of age variable
}